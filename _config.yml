defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"

base_path: "//gios-asu.github.io/"

kramdown:
  input: GFM
  auto_ids: true
highlighter: pygment